# ImplementationPlan — Reflex

**Version:** 1.1 · Team assumption: 3 engineers (A: Data/Sim/Eval · B: Backend/Agent · C: Frontend/Design; A doubles as AI engineer). Calendar: **4 working days** (compressible 3, stretch 5).

Task format: `ID · P0/P1/P2 · Deps · Complexity(S/M/L) · DoD`.

> **STATUS SNAPSHOT (authoritative — supersedes "untouched plan" reading; updated post official run, 2026-08-24):**
> **35 [x] done · 19 partial ([/] 18 + [~] 1) · 5 [ ] open — of 59 tracked tasks** *(recounted v1.6; incl. TASK-059, added by the v1.6 audit and tracked in `7.Tracker.md` Phase 10).*
> *(Post-run update: TASK-033 executed [official artifacts committed at `eval/results/20260826T105147Z/`];
> TASK-036 closed [results commit + docs/limitations.md]; TASK-053 closed [Amendment-1 tags cut, run executed on amended mixture].)*
> The original phase tables below are PRESERVED unchanged for their DoD definitions; the table
> here is the operational truth. Remaining roadmap = every non-✅ row.

| Task | St | One-line actual state |
|---|---|---|
| 001 | 🟡 | repo+CI yaml done; CI never executed on GitHub; no PR template |
| 002 | 🟡 | pg+redis healthy (max_conn=300); compose defines api/workers/eval builds + **web service added v1.3** — app images still never BUILT |
| 003 | ✅ | applied; agent-role lockout TESTED (42501 on replay.sim_* SELECT + ledger UPDATE) |
| 004 | ✅ | Vite+React18+TS strict+Tailwind tokens+router; tsc/build/vitest green |
| 005 | ✅ | PROTOCOL committed + tag `eval-preregistered-v1` BEFORE any results |
| 006 | ✅ | enums/schemas/SMs(+DB triggers)/clock/PII/money/models; transition tests green |
| 007 | ✅ | sha256 chain append/verify/tamper/reorder tests green; fast-mode for Proof |
| 008 | ✅ | HMAC-401/dup-200/zero-amount tests; storm integration 1000→214, 0 dupes |
| 009 | 🟡 | 3 worker roles coded; dx shards consumer; `_mode` crash FIXED; live loop unverified |
| 010 | 🟡 | RP-TM client coded (rzp_test_ guard, backoff 1/4/16); backoff/park unit tests missing; never called live [UNVERIFIED] |
| 011 | ✅ | JWT+pbkdf2 seeded users; RBAC matrix test green; rate buckets per route |
| 012 | ✅ | rules engine; rules coverage 89.6% on the 500-case degraded holdout (eval/results/dx_holdout/report.json); unit gate ≥70% |
| 013 | ✅ | Shield 7 checks fail-closed; adversarial incl. at-limit edges; isolation lint green |
| 014 | ✅ | ledger-first dispatch; idempotency UNIQUE; golden-path integration green |
| 015 | ✅ | WA/SMS/email/voice sims deterministic under seed; `[SIMULATED]` metadata |
| 016 | ✅ | watch windows/attribution/supersede/expiry; 100% terminals in integration run |
| 017 | 🟡 | dashboard+drawers+SSE hook built (tsc/build/vitest green); SSE live round-trip unverified |
| 018 | 🟡 | onboarding routes+page; RP ₹1 check requires real keys [TBD keys] |
| 019 | ✅ | versioned prompts (reply_classify now **v2**); hashes recorded to llm_calls |
| 020 | 🟡 | LLM tail full path runs LLM-absent (degraded-safe); ≥85% holdout SKIPPED w/o key |
| 021 | ✅ | skeleton→LLM→inject→validator→fallback; 100% digit-corpus rejection tested |
| 022 | ✅ | classifier keyword-gate-first; COMPLAIN precision ≥95% AND recall ≥90% offline gates green |
| 023 | ✅ | PII scrub + scanner test over llm_calls post-run green |
| 024 | ✅ | EV v1 priors; 4-term persisted; negative-EV STOP w/ math |
| 025 | ✅ | diagnose→rank→Shield→schedule/approve/park integrated (shared with workers) |
| 026 | ✅ | triggers+queue API+UI; timeout auto-decline observed in runs |
| 027 | 🟡 | modes/kill-switch backend+UI; drain MEASURED v1.3: 25.5ms/500 actions (harness); live-button pass pending |
| 028 | 🟡 | DEGRADED arm proven in-process (stamps, zero drops); live outage drill pending |
| 029 | 🟡 | sklearn trainer written; NEVER RUN (P1) |
| 030 | 🟡 | 4 inject endpoints implemented via real paths; none exercised live |
| 031 | ✅ | generator(byte-identical)+demo-slice contract+pipeline+arms+sims bridge |
| 032 | ✅ | tuning grid RAN+committed (dev seeds {7,99}: all tie 19.63% ⇒ default confirmed) |
| 033 | ✅ | **EXECUTED 2026-08-24:** official N=3000×3-seed×8-arm run completed under `eval-preregistered-v1`; `results.json`+`tables.md` w/ bootstrap CIs committed at `eval/results/20260826T105147Z/` (earlier host-Docker/port blockers root-caused & fixed — session-scoped advisory lock + serial-arm default, host port 15432; superseded attempts archived) |
| 034 | 🟡 | reproduce.sh written; never executed clean-clone |
| 035 | 🟡 | Results page built (CI brackets, ablation bars); never rendered vs data |
| 036 | ✅ | results commit + docs/limitations.md DONE — results at `eval/results/20260826T105147Z/` [SIMULATED]; honest-record doc created (incl. published losing cohort: 3,526 correct declines) |
| 037 | ✅ | RBAC matrix/rate-buckets/idempotency-unique tested |
| 038 | ✅ | sig/tamper/isolation/PII/injection suites green; gitleaks CI job wired — local scan pending (binary not installed on build host) |
| 039 | 🟡 | grants tested (append-only verified); export endpoints shipped (/api/episodes/export, /api/ledger/export; CSV+JSON watermarked); UI wiring unverified |
| 040 | 🟡 | tokens/chips/badges/formatINR shipped; visual QA vs Design.md pending |
| 041 | ✅ | episode/EV/ledger drawers complete w/ rationale+snapshot |
| 042 | ✅ | Action Preview Card component used in Approvals |
| 043 | 🟡 | empty/loading/error + aria-live coded; axe/keyboard pass not run |
| 044 | 🟡 | /audit+/ops built; never clicked against live stack |
| 045 | ✅ | demo slice contract unit-verified (214/₹2,41,000/₹48k/complaint); re-fit green after RISK_HELD mixture change; runtime start script untested |
| 046 | ❌ | rehearsals/drill (human) |
| 047 | 🟡 | README/CONTRIBUTING/MANUAL_STEPS created (v1.3); pitch video + hero GIF pending |
| 048 | ❌ | backup recording/restore script |
| 049 | ✅ | **backend suite 164 passed / 0 failed [LOCAL RUN this commit; no CI artifact yet]** against PG16+Redis containers (e2e+ai_live excluded); CI-on-GitHub execution still pending |
| 050 | ❌ | acceptance walkthrough recording |
| 051 | ✅ | informal sync during build; v1.3 formal audit sync applied to all docs (status synced v1.6) |
| 052 | ❌ | submission packaging |
| 053 | ✅ | RISK_HELD mixture landed + demo slice re-fit green; Amendment-1 tags cut and official run executed on the amended mixture (2026-08-24) |
| 054 | ✅ | reply prompt v2 + confidence field + confidence-gated fallback; COMPLAIN recall ≥90% gate green offline |
| 055 | ✅ | migration 0002 `actions.llm_call_id` FK + dispatcher capture; provenance join queryable; suite green |
| 056 | 🟡 | RP duplicate-link guard CODE-COMPLETE (client guard shipped in packages/connectors/razorpay.py); live verification owed |
| 057 | ✅ | AI-3 spelled-out-number blocklist (Hinglish loophole) in MessageSpanValidator; template fallback; regression-tested |
| 058 | ✅ | AI-4 deterministic sarcasm/negative-polarity gate before LLM acceptance → safe AMBIGUOUS; regression-tested |

**Critical path progress:** 003✅→006✅→008✅→009🟡→012✅/014✅→024✅/025✅→**031✅→033✅(executed 2026-08-24)→034🟡**→047🟡

---

## Phase 0 — Project Setup (Day 0, ~2h)

| Task | Description | Files/Module | P | Deps | C | DoD |
|---|---|---|---|---|---|---|
| TASK-001 | Repo init, branches, PR template, CI skeleton | repo root, `.github/` | P0 | — | S | CI runs hello-tests on PR |
| TASK-002 | docker-compose: postgres/redis/api/web; env loader; `.env.example` | `docker-compose.yml` | P0 | 001 | S | `make up` healthy; `/healthz` 200 |
| TASK-003 | Alembic baseline migration (Schema V1: schemas, tables, enums, grants incl. agent-role lockout) | `alembic/` | P0 | 002 | M | migration applies; agent role cannot SELECT `replay.sim_*` (test) |
| TASK-004 | Frontend scaffold: Vite+React+TS+Tailwind+shadcn, routing, auth shells | `apps/web/` | P0 | 002 | S | routes render; login stub works |
| TASK-005 | **Write `eval/PROTOCOL.md` + tag `eval-preregistered-v1` BEFORE any results** | `eval/` | **P0-critical** | 003 | S | tag exists in git history prior to first eval run |

## Phase 1 — Foundation (Day 0.5–1)

| Task | Description | Module | P | Deps | C | DoD |
|---|---|---|---|---|---|---|
| TASK-006 | Core domain: enums, Pydantic schemas, episode state machine (+ transition tests) | `packages/core` | P0 | 003 | M | 100% legal transitions tested; illegal raise |
| TASK-007 | Ledger service: append, hash chain, verify endpoint logic | `packages/ledger` | P0 | 006 | M | chain verify passes; tamper test fails loudly |
| TASK-008 | Ingestion API: webhook route, HMAC verify, dedup, normalize, episode create | `apps/api` | P0 | 006 | M | sig test; storm test (1k dupes → 214) |
| TASK-009 | Redis Streams plumbing + worker skeleton (diagnosis/decision/outcome consumers) | `apps/workers` | P0 | 008 | M | events flow end-to-end to no-op handlers |
| TASK-010 | RP-TM client (orders, payment links) with idempotency + timeout/backoff | `packages/connectors` | P0 | 002 | M | retry/park unit tests; `[TEST MODE]` badge data |
| TASK-011 | Auth + RBAC middleware (4 roles) + seeded users | `apps/api` | P0 | 008 | S | RBAC matrix test green |

## Phase 2 — Core Product (Day 1–1.5)

| Task | Description | Module | P | Deps | C | DoD |
|---|---|---|---|---|---|---|
| TASK-012 | Rules diagnosis engine + canonical taxonomy + string corpus | `apps/workers` + `data/generators` | P0 | 009 | M | ≥70% of corpus classified; p95<100ms |
| TASK-013 | Shield v1: all seven checks + guardrail_snapshot; import-lint isolation | `packages/shield` | P0 | 006 | M | adversarial suite 0 violations; isolation test |
| TASK-014 | Executor dispatch path: schedule→Shield→dispatch→ledger, idempotent | `apps/workers` | P0 | 010, 013 | L | replayed dispatch → zero dupes (test) |
| TASK-015 | Channel simulators (WA/SMS/email/voice) + stochastic response engine | `packages/connectors` | P0 | 014 | L | deterministic under seed; `[SIMULATED]` metadata |
| TASK-016 | Outcome worker: watch windows, attribution, terminal transitions, counters | `apps/workers` | P0 | 014 | M | golden path closes episode in sim ≤5s |
| TASK-017 | Dashboard shell: control bar, counters, stream, SSE | `apps/web` | P0 | 009, 011 | L | live rows <2s latency at ×100 |
| TASK-018 | Onboarding flow (keys check, webhook setup, guardrail defaults, mode) | `apps/web` | P1 | 011 | S | fresh boot → configured state |

## Phase 3 — AI (Day 1.5–2)

| Task | Description | Module | P | Deps | C | DoD |
|---|---|---|---|---|---|---|
| TASK-019 | Prompts v1 (diagnosis, message, reply) in `packages/prompts` + versioning | `packages/prompts` | P0 | 012 | S | hashes recorded; eval suite wired |
| TASK-020 | LLM diagnosis tail: `<data>` wrapping, schema validation, retry→UNKNOWN, cache | `apps/workers` | P0 | 019 | M | 500-case holdout ≥85%; injection corpus safe |
| TASK-021 | Message generation: skeleton→LLM→DB-injection→**validator**→template fallback | `apps/workers` | P0 | 019 | M | digit-corpus 100% rejected; diffs logged |
| TASK-022 | Reply classifier + suppression path (AI-4 + keyword gate) | `apps/workers` | P0 | 019 | S | COMPLAIN precision ≥95%; zero post-complaint contact |
| TASK-023 | PII scrubber + no-PII-in-prompt test | `packages/core` | P0 | 020 | S | scanner test green |

## Phase 4 — Agentic Workflow (Day 2–2.5)

| Task | Description | Module | P | Deps | C | DoD |
|---|---|---|---|---|---|---|
| TASK-024 | EV policy v1 (prior coefficients from calibration doc) + candidate persistence w/ 4-term breakdown | `packages/brain` | P0 | 012 | M | EV math unit tests incl. negative-EV STOP |
| TASK-025 | Decision worker integration: diagnose→rank→Shield→schedule/approve/park | `apps/workers` | P0 | 014, 024 | L | full golden path; blocked paths ledgered |
| TASK-026 | Approval queue: triggers, timeout auto-decline, decide API, UI | api + web | P0 | 025 | M | matrix test; fail-closed timeout |
| TASK-027 | Kill switch + modes + SSE banners; drain semantics | api + web | P0 | 025 | M | halt stops dispatch ≤1s (test) |
| TASK-028 | **Degraded mode**: health-check, fallback dx+policy, `DEGRADED` stamping, banner | `apps/workers` + web | P0 | 020, 024 | M | outage injection: zero dropped episodes |
| TASK-029 | Policy v2 trainer (logistic on replay outcomes) + version switch + learning curve data | `packages/brain` | P1 | 031 | M | v2 beats v1 on held-out batch |
| TASK-030 | Failure injections as first-class scenarios (`/api/control/inject/*`) | api | P0 | 027, 028 | S | all three injectable + recoverable |

## Phase 5 — Metrics / Evaluation (Day 2.5–3) **[runs parallel with Phase 7]**

| Task | Description | Module | P | Deps | C | DoD |
|---|---|---|---|---|---|---|
| TASK-031 | Replay engine: batch generator, hidden sim params, time acceleration, arm policies B0/B1 | `apps/eval` | **P0-critical** | 015, 016 | L | same seed ⇒ identical batch; agent role can't read truth |
| TASK-032 | B1 honest tuning script + documented search | `apps/eval` | P0 | 031 | M | tuning notes committed |
| TASK-033 | Eval runner: arms + ablations A1–A4, bootstrap CIs, JSON+table outputs | `apps/eval` | P0 | 031 | M | 3×3,000 <10min; CIs on all metrics |
| TASK-034 | `reproduce.sh` end-to-end from clean clone | `eval/` | P0 | 033 | S | fresh clone → results <15min |
| TASK-035 | Results UI: arm table, ablation bars, learning curve, `[SIMULATED]` labeling | web | P0 | 033 | M | every number shows CI+arm+seed |
| TASK-036 | Commit results + write `docs/limitations.md` (incl. losing cohort) | docs | P0 | 034 | S | limitations honest + linked from README |

## Phase 6 — Security (Day 3, ~0.5d)

| Task | Description | P | Deps | C | DoD |
|---|---|---|---|---|---|
| TASK-037 | RBAC matrix tests, rate limits on all routes, idempotency on POSTs | P0 | 011 | S | matrix green; limits enforced |
| TASK-038 | Security suite: sig, tamper, secrets scan (gitleaks), prompt-injection corpus in CI | P0 | 020, 007 | S | CI blocking |
| TASK-039 | Ledger grants hardening (no UPDATE/DELETE), export labeling | P0 | 007 | S | grants verified test |

## Phase 7 — UI Polish (Day 3, parallel)

| Task | Description | P | Deps | C | DoD |
|---|---|---|---|---|---|
| TASK-040 | Design-system pass per `4. Design.md` (tokens, chips, badges, tabular nums, ₹ grouping) | P0 | 017 | M | visual QA vs Design.md |
| TASK-041 | Episode/EV/Ledger drawers with full rationale + guardrail snapshot | P0 | 025 | M | rationale ≤2 clicks from any row |
| TASK-042 | Action Preview Card component (WHAT/WHY/IMPACT/RISK/GATE/APPROVAL/REVERSIBILITY) | P0 | 041 | S | used in approvals + drawer |
| TASK-043 | Empty/loading/error states + accessibility pass (contrast, keyboard, aria-live) | P1 | 040 | M | axe scan clean; keyboard demo works |
| TASK-044 | `/audit` + `/ops` screens | P1 | 027, 030 | M | verify-chain UI; injection console |

## Phase 8 — Demo Preparation (Day 3.5)

| Task | Description | P | Deps | C | DoD |
|---|---|---|---|---|---|
| TASK-045 | Demo slice seed `demo-7` (214 eps/₹2,41,000; incl. ₹48k approval + complaint trajectory) | P0 | 031 | S | slice deterministic; matches AppFlow §13 |
| TASK-046 | Rehearse 5-min pitch ×3 with timer; record failover drill (outage switch) | P0 | 030, 045 | M | ≤5:00 with margin; outage lands |
| TASK-047 | Pitch video (5 min), README hero GIF, metrics table `[SIMULATED]` | P0 | 036 | M | video crisp; README ≤10-min onboarding claim verified |
| TASK-048 | Backup plan: pre-recorded full-demo video + DB snapshot restore script | P0 | 046 | S | restore-to-demo ≤2min |

## Phase 9 — Final QA (Day 4)

| Task | Description | P | Deps | C | DoD |
|---|---|---|---|---|---|
| TASK-049 | Full regression: unit/integration/api/ai/e2e/security/load suites | P0 | all | M | green on clean clone CI |
| TASK-050 | PRD §20 acceptance criteria walkthrough (10 items) — recorded | P0 | all | S | all 10 ✅ with evidence |
| TASK-051 | Cross-doc consistency check (names, numbers, flows across 8 docs) | P0 | docs | S | zero mismatches |
| TASK-052 | GitHub submission: repo public, README complete, submission checklist (Tracker §) | P0 | all | S | checklist 100% |

## Parallel Workstreams

| Lane | Owner | Days 0.5–2.5 focus | Days 2.5–4 focus |
|---|---|---|---|
| **A — Data/Sim/AI** | Eng A | generators, calibration doc, simulator, prompts, LLM components | eval runs, ablations, results docs, video metrics |
| **B — Backend/Agent** | Eng B | ingestion, workers, Shield, executors, policy, degraded mode | security suite, load, ledger hardening, injections |
| **C — Frontend** | Eng C | dashboard/SSE/drawers/approvals/controls | results UI, polish, a11y, demo script builds |

Shared interface contract frozen Day 0.5: API routes (TechSpec §10) + SSE event names + enums (Schema §5).

## Critical Path

`TASK-003 → 006 → 008 → 009 → 012/014 → 024/025 → 031 → 033 → 034 → 047`
**The two schedule-killers:** replay engine (031) and eval runs (033). Start 031 design Day 0.5 even while Phase 1 runs.

## MVP Cut Line

> **If time runs out, stop here — and no further:**
> Cut in this order: (1) voice-call sim (use WA/SMS), (2) policy v2 learning (ship v1 priors; drop learning-curve chart), (3) ablations A3/A4 (keep A1 rules-only diagnosis and A2 EV-off — the AI-necessity proof), (4) onboarding flow (pre-seeded config), (5) `/ops` niceties.
> **NEVER cut:** Shield · ledger · B0/B1 comparison · degraded mode · injection demos · `[SIMULATED]` labeling · reproduce.sh · pre-registration tag.

## Build Order (exact)

*(v1.2 audit fix P1-5: **029** and **036** restored — both were silently absent from this
sequence even though 036 is P0 and a prerequisite of 047.)*

005 (tag protocol!) -> 001-004 -> 006-007 -> 008-011 -> 012/013 parallel -> 014 -> 015/016 -> 017 parallel -> 024 -> 025 -> 026/027/028 -> 019-023 (AI slots in) -> **031 (critical)** -> **029 (P1 - cut-eligible per MVP Cut Line; sequenced here if kept)** -> 030/032/033 -> 034/035 -> **036 (P0 - prerequisite of 047)** -> 037-039 -> 040-044 -> 045-048 -> 049-052.

---

## Phase 10 — Audit Remediation Tasks (v1.2, from independent Claude audit)

| Task | Description | P | Deps | C | DoD |
|---|---|---|---|---|---|
| TASK-053 | Add `RISK_HELD` to synthetic mixture (rebalance shares), re-fit demo slice to 214/Rs2,41,000, re-verify byte-identity, tag protocol amendment BEFORE official run | P1 | 031 | M | mixture includes RISK_HELD; slice tests green; amendment tag precedes results |
| TASK-054 | Add `confidence` to reply-classifier schema + confidence-gated fallback (mirror AI-1); add COMPLAIN recall >=90% gate to AI suite | P1 | 022 | S | recall gate green; confidence path incl. fallback tested |
| TASK-055 | Add `actions.llm_call_id UUID FK null` (migration + model + dispatcher capture) for message-gen provenance | P2 | 003 | S | provenance join queryable; existing tests green |
| TASK-056 | Verify/document RP-side idempotency behavior on timeout-retry (test-mode experiment); record finding in docs/limitations.md | P2 | 010 | S | written finding; connector note updated |

*(v1.3 status: **054 ✅ and 055 ✅ — implemented + tested.** 053 now ✅ complete
[mixture rebalanced INSUFFICIENT_FUNDS 34→32 / RISK_HELD 2%; demo slice re-fit verified;
byte-identity green; Amendment-1 tags cut] — the official run executed on the amended
mixture on 2026-08-24 (`eval/results/20260826T105147Z/`). 056 🟡 code-complete
(client guard shipped); live verification owed. TASK-057/058 ✅ shipped (AI blocklist +
sarcasm/negative-polarity gate). The reply-classify prompt moved to v2 per Rules §2.7 versioning.)*
