# Reflex — AI Revenue Recovery Agent (Razorpay AI Buildathon, Track 03)

> Reflex is a bounded, root-cause-diagnosing payment-recovery agent: it ingests failed-payment
> webhooks, diagnoses each failure's root cause (rules first, LLM only for an ambiguous tail),
> ranks candidate interventions by expected value, enforces non-overridable deterministic
> guardrails, recovers via Razorpay TEST-MODE payment links, and writes every action to a
> hash-chained ledger. Tagline: "Recover more, annoy less, prove everything." Governing
> principle: "AI proposes, deterministic code disposes." Official pre-registered evaluation
> [SIMULATED] (2026-08-30 artifact): Reflex recovered **33.83% [95% CI 29.36–38.75]** of failed
> value vs **25.06% [20.92–29.43]** for a tuned-naive baseline and **4.11% [2.72–5.82]** organic —
> incremental **+8.77 pp [4.49–13.28]**, at ₹0.24 cost per ₹100 recovered and 0.2% complaint rate.

## What this project is

Failed recurring payments in India (UPI/card/mandate auto-debits) fail for many different root
causes — insufficient funds near salary day, issuer downtime, expired cards, revoked mandates —
and generic retry-and-spam recovery ignores those causes, wasting money and annoying customers.
Reflex's thesis: most failed-payment revenue doesn't need a better retry button, it needs a
correct diagnosis followed by a bounded, cheapest-appropriate action. An LLM is used only where
its strength lies (classifying messy, ambiguous bank-decline text); every decision that touches
money or customers is made by deterministic, auditable code. The entire system is evaluated by a
pre-registered protocol whose hidden simulation parameters are structurally invisible to the
agent (database role separation).

## Critical honesty context (read first)

- Everything measured is **[SIMULATED]** — synthetic customers, simulated channels, Razorpay
  TEST MODE only. No real money moved, no real customers contacted, ever.
- Pre-registered design target was ≥ +15 pp incremental over the tuned baseline. The measured
  official run delivered **+8.77 pp [4.49–13.28]** — a real effect (CI excludes zero) that
  **MISSED the ≥ +15 pp G1 gate**, and the miss is reported verbatim — never tuned away.
- The official run was keyed (honest AI result): the LLM tail executed WITH a working key
  (temperature 0) and honestly classified the synthetic ambiguous-tail strings as
  UNKNOWN_AMBIGUOUS — matching the conservative fallback — so reflex ≡ A1 ≡ DEGRADED at
  33.83% (safety proven under real provider conditions; differentiation requires real-world
  decline strings). A pre-keyed attempt is acknowledged in §3 of `docs/limitations.md`.

## Architecture

Six subsystems, one deployable unit:

- **Pulse** — webhook ingestion: HMAC verification, provider-event-id dedup, episode creation;
  rules-first diagnosis (regex/lookup covers ~89.6% of a 500-case holdout) with an LLM tail for
  the ambiguous remainder.
- **Brain** — EV policy: `EV = p_recover × amount − channel_cost − annoyance_penalty`; all four
  terms persisted per candidate intervention; v1 logistic priors frozen; v2 trainer written.
- **Shield** — deterministic, fail-closed guardrails: max 4 actions/episode, 2 contacts/customer/
  day, ₹5,000/day spend budget, quiet hours 21:00–09:00 IST, suppression/DND lists, human
  approval required above ₹50,000, global kill switch.
- **Hands** — Razorpay TEST MODE adapters (orders, payment links) plus explicitly labeled
  [SIMULATED] channel simulators (WhatsApp/SMS/email/voice). Rejects any non-`rzp_test_` key
  (`TestModeViolation`). Duplicate payment-link guard: reference-id lookup before create and
  after timeouts.
- **Ledger** — append-only hash-chained action log: `sha256(seq ‖ prev_hash ‖ event::text)`
  computed server-side inside a single atomic INSERT (pgcrypto), tamper-detection endpoint,
  app DB role limited to INSERT/SELECT (no UPDATE/DELETE grants). Append and verify hash the
  exact same stored bytes by construction.
- **Proof** — replay engine, hidden-parameter outcome simulator, honestly tuned baselines, and
  the pre-registered evaluation harness (`eval/PROTOCOL.md`).

AI components: **AI-1** diagnosis NLU (rules + LLM tail), **AI-2** EV policy, **AI-3** message
generation (slot-skeleton phrasing; validator rejects any digit/URL/₹/UPI token AND spelled-out
number words in Hinglish or English; template fallback on violation), **AI-4** reply classifier
(PROMISE/REFUSE/COMPLAINT/OPTOUT/PAYING/AMBIGUOUS; keyword gate first; sarcasm/negative-polarity
gate before LLM acceptance; COMPLAINT precision ≥95% and recall ≥90% gates green offline).

## Anti-cheat architecture

Postgres role separation makes rigging structurally difficult: the agent role (`reflex_agent`)
physically cannot SELECT the hidden simulator tables (`replay.sim_customers`, `replay.sim_events`) —
verified by SQLSTATE-42501 tests. The evaluation protocol was committed and git-tagged
(`eval-preregistered-v1`, plus amendments) BEFORE any results existed; the gate is checked at
eval time via local git history, `git ls-remote` against GitHub, or the REST API (fails closed,
cached briefly). Baseline B1 is honestly tuned (documented grid search in
`eval/results/b1_tuning.json`). A losing cohort is published. Reproduction is proven: two
complete independent official runs on different days produced byte-identical per-seed results.

## Measured results (official pre-registered run, ALL [SIMULATED])

Protocol `eval-preregistered-v1`, seed 42, N=3,000 episodes × 8 arms (full protocol matrix),
bootstrap 95% CIs (1,000 episode-level resamples). Artifacts: `eval/results/20260830T105923Z/`
(results.json + tables.md). These runs are loaded in the deployed product's Results page.

| Arm | Recovery % [95% CI] | Cost / ₹100 | Complaints | Median TTR |
|---|---|---|---|---|
| B0 do-nothing | 4.11 [2.72, 5.82] | — | 0% | 39.2 h |
| B1 tuned naive | 25.06 [20.92, 29.43] | ₹0.12 | 0.533% | 5.2 h |
| **Reflex** | **33.83 [29.36, 38.75]** | **₹0.24** | **0.2%** | **10.1 h** |

- Incremental recovery vs B1: **+8.77 pp [4.49–13.28]** (CI excludes zero — the effect is real).
- Gate scorecard: G2 cost ≤ ₹3.5 **PASS** · G3 complaints < 0.5% **PASS** · G5 same-seed
  reproduction **PASS (proven)** · **G1 ≥ +15 pp MISSED at +8.77 pp — reported honestly** ·
  G6 vacuous without LLM differentiation.
- Ablations: A1 rules-only 33.83% (= full; the LLM conservatively agrees with the fallback on
  the synthetic ambiguous tail — see limitations §3b) · **A2 EV-off 39.93% [34.98, 44.91]** —
  higher recovery at ~2× the cost (₹0.49 vs ₹0.24 per ₹100) and ~1.8× the complaints
  (0.367% vs 0.2%), committed as an honest negative result
  (over-pessimistic v1 priors; remedy is protocol-disciplined v2 recalibration) · A3 static
  templates 33.83% · **A4 no-timing 27.97% [23.9, 32.36]** ⇒ salary-cycle timing is the
  strongest measured lever (≈ +5.9 pp) · DEGRADED 33.83%.
- Losing cohort: **1,189 episodes** where Reflex correctly DECLINED to act (contact cost >
  expected value); B1 spends on all of them.

## Quickstart

```
git clone https://github.com/abhinav-phi/reflex.git && cd reflex
cp .env.example .env        # optional: add LLM_API_KEY (system runs LLM-absent-safe without it)
make up                     # docker compose: postgres(15432)+redis+api+workers+web + migrations
make seed                   # idempotent: 4 users, merchant "SipDaily", policy v1, corpora
make demo                   # 214 episodes / ₹2,41,000 failed value, seed demo-7, ×100 speed
```

- UI: http://localhost:8080 (Docker) or http://localhost:5173 (dev). API: :8899 (dev) /
  :8000 (Docker). Postgres host port: 15432 (Windows reserves port ranges covering 5432).
- Seeded logins (password `reflex-demo`): admin@reflex.dev · operator@reflex.dev ·
  approver@reflex.dev · viewer@reflex.dev.
- Official evaluation: `./eval/reproduce.sh` (refuses to run if the pre-registration tag is
  missing). Backend tests: `make test` (CI backend job green).

## Production behavior notes (current build)

- **Ledger:** hashes are computed server-side inside a single atomic INSERT over the
  jsonb-normalized event text (`sha256(seq ‖ prev_hash ‖ event::text)` via pgcrypto) — append
  and verify agree by construction, concurrent writers cannot fork the chain, and the chain
  stayed valid through live demos (31,000+ events; production chain re-stamped once on
  2026-09-01 to repair concurrent eval bulk-load races, then migrated — verify now reports
  `valid:true` over 204k+ rows). Per-episode trails verify each row against its own global
  predecessor, so interleaved replay-era episodes verify correctly.
- **Counters** are recomputed from Postgres at startup — Ops shows real totals after redeploys.
- **Drive end:** when a replay finishes, all non-terminal episodes are force-resolved (72h
  expiry + fail-closed approval timeouts, ledgered) and the sim clock resets to wall time —
  nothing stays frozen in "observing".
- **Security hardening:** random `JWT_SECRET` in production, `/debug/*` admin-only
  (migrations POST-only), `/metrics` viewer-gated, `/docs`+`/openapi.json` disabled unless
  `DEBUG=1`, CORS limited to the deployed origins, CSP headers on the static app.
- **Eval gate:** the pre-registration tag is checked via git history, `git ls-remote`, or the
  GitHub API (cached; transient failures never block permanently) — "Run eval" works from the
  deployed container.
- **SSE:** real Redis pubsub delivers live events; the frontend exchanges the session JWT for
  a 60-second stream credential so the JWT never rides the URL; reads use a dedicated thread pool.

## Failure-handling demos (real injections through real paths)

- `llm_outage` → DEGRADED banner, rules-only fallback, zero dropped episodes, actions stamped
  DEGRADED.
- `webhook_storm` → 1,000 events collapse to 214 episodes via provider-event-id dedup.
- `complaint` injection → instant global suppression + human handoff + STOPPED_CUSTOMER.
- Kill switch → confirmation modal → system-wide halt, deliberate re-enable required.

## Key files

- `README.md` — overview, metrics, post-mortem ("what broke and how we fixed it")
- `MANUAL_STEPS.md` — operator runbook: setup, demo playbook, injections, troubleshooting
- `CONTRIBUTING.md` — setup, conventions, non-negotiable fintech/AI safety rules
- `docs/limitations.md` — THE honest record: gate scorecard, caveats, negative results
- `eval/PROTOCOL.md` — pre-registered evaluation protocol + amendment history
- `eval/results/20260830T105923Z/` — official pre-registered results (results.json + tables.md)
- `docs/1. PRD.md` … `docs/8. Rules.md` — full design documentation (8 canonical files)
- `tests/ai/test_diagnosis_accuracy.py` — 500-case diagnosis holdout (writes confusion matrix
  to `eval/results/dx_holdout/`)
- `LICENSE` — Apache-2.0

## What an evaluator should check (suggested verification path)

1. Open the live deployment (below) or run the quickstart; log in; start the demo replay;
   watch live SSE updates on the dashboard (green "connected" pill).
2. Trigger the failure injections from `/ops` (`llm_outage`, `webhook_storm`, `complaint`);
   verify zero dropped episodes.
3. Verify the anti-cheat boundary in Postgres:
   `SELECT has_table_privilege('reflex_agent', 'replay.sim_customers', 'SELECT');` → must be `false`.
4. Verify the ledger chain: `/audit → Verify chain` or `GET /api/ledger/verify` →
   `{"valid": true, ...}` — valid even while a demo is running.
5. Cross-check any quoted number against `eval/results/20260830T105923Z/results.json`.
6. Read `docs/limitations.md` before repeating ANY metric — especially the G1 miss and the
   A2 anomaly.

## Deployment (live)

A live instance of this exact code runs end-to-end (all data [SIMULATED], TEST MODE):

- **UI:** https://reflex-recover.vercel.app (Vercel static SPA + CSP headers)
- **API:** https://reflex-api-production.up.railway.app (Railway Docker container: FastAPI +
  embedded workers + Redis on the Railway private network via `REDIS_URL`)
- **PostgreSQL:** Aiven Free (`reflex-pg`, Amsterdam — same city as the API; 1 GB disk,
  1 vCPU/1 GB RAM, automated backups) via `DATABASE_URL` with `sslmode=require`. Migrated
  from Railway free-tier Postgres on 2026-09-01 after its fixed 500 MB volume filled and
  crash recovery could no longer write WAL (bit-exact migration: all table counts + a
  9-value ledger fingerprint verified). API connection pools are capped for Aiven's
  `max_connections=20`. Aiven powers the service off after ~1 week of zero traffic —
  Power On from the console before a demo (runbook in `MIGRATION.md`).

Logins (password `reflex-demo`): `admin@reflex.dev` / `approver@` / `operator@` / `viewer@`.
Infra wiring (Vercel build env `VITE_REFLEX_API` for Production+Preview, Railway `DATABASE_URL`
+ `REDIS_URL`, CORS allow-list, `${PORT:-8000}` Dockerfile) is documented in `MIGRATION.md`.
The production build refuses to compile without `VITE_REFLEX_API` (same-origin regression
guard); GitHub Actions skips that guard because its bundle is never deployed. Branch flow:
work lands on `master`; a workflow fast-forwards `main` on every master push.

## Known limitations (summary)

- G1 (incremental ≥ +15 pp) MISSED at +8.77 pp — real effect, smaller than target.
- LLM-tail recovery value unmeasured on synthetic data: an honest model matches the
  conservative fallback on deliberately-unclassifiable strings (§3b of limitations).
- A2 EV-off anomaly (39.93% > 33.83%) pending v2 prior recalibration.
- Same-seed reproduction drift existed pre-TASK-061; fixed and re-proven (see G5 above).
- Live Razorpay test-mode observation of the duplicate-payment-link guard still owed.

## Non-goals

Real customer contact; irreversible money movement; replacing gateway retry logic; live API
keys anywhere in the evaluation path; auto-retraining without a fresh pre-registration.
