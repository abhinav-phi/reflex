# PRD — Reflex

**Version:** 1.1 · **Status:** Build-ready → **BUILD ~69% EFFECTIVE** (26 tasks ✅ / 20 🟡 / 6 ❌ — see §22 Implementation Status) · **Owner:** Team Reflex
**Track:** Razorpay AI Buildathon — Track 03 (AI Revenue Recovery)

> **v1.1 SYNC NOTE (post build-sprint):** Sections 1–15 remain the product specification.
> §16 metrics are still *design targets* — **no official evaluation has been run yet**
> (official N=3000×3-seed run blocked by host-environment instability; harness itself verified
> at smoke scale). §20 acceptance criteria are annotated with current pass/fail state.
> §22 is the authoritative FR-by-FR implementation status map synced to the codebase.

---

## 1. Product Overview

| Field | Value |
|---|---|
| **Product name** | **Reflex** (renamed from working title "Recover" — avoids collision with Baremetrics "Recover") |
| **One-line description** | A bounded, root-cause-diagnosing recovery agent that wins back failed payments and failed mandates with the cheapest intervention most likely to work — measured, audited, and safe by construction. |
| **Winning track** | 03 — AI Revenue Recovery |
| **Category** | Merchant-side fintech automation · Agentic revenue operations |
| **Product thesis** | Most failed-payment revenue doesn't need a better retry button — it needs a **correct diagnosis**. "Insufficient balance," "issuer downtime," "mandate revoked," and "card expired" are different problems needing different fixes, in the language and channel the customer responds to, inside hard limits that neither the merchant nor the AI can accidentally blow through. |

## 2. Executive Summary

Indian subscription/D2C merchants lose recurring revenue to payment failures (UPI, cards, e-mandates/NACH). Today they respond with either silence (revenue leaks) or dumb blast messaging (revenue leaks **plus** annoyed customers). Razorpay's own recovery tooling exists but is described publicly at one-line depth; the buildathon bar demands measured recovery, stopping rules, and audit trails — which no thin shipped blurb demonstrates.

Reflex is a merchant-side agent on top of **Razorpay test-mode APIs**: it ingests failure events, classifies each into a **canonical root cause** (rules first, LLM only for the ambiguous tail), scores every candidate intervention by **expected value** (propensity × amount − channel cost − annoyance penalty), passes every action through a deterministic **Shield** guardrail module, executes via retries / payment links / simulated channels (WhatsApp, SMS, email, Hinglish voice), observes outcomes, adapts its policy, and **stops or escalates** when rules or EV say so. Every decision is written to a **hash-chained action ledger**.

Value is proven on a **pre-registered, reproducible evaluation**: 3,000-episode synthetic replay, three arms (do-nothing, tuned naive, Reflex), ablations, bootstrap confidence intervals, one-command reproduction. Simulation results are always labeled as simulation.

**Design principle (say it in every pitch):** *AI proposes, deterministic code disposes.* The LLM never authors an amount, link, deadline, or authorization.

## 3. Problem Statement

**Exact problem.** A material share of payment attempts and recurring-debit mandates fail (card declines, UPI failures, eNACH/UPI AutoPay mandate failures). Merchants either don't follow up or follow up indiscriminately. Recovery today is manual, unsophisticated, unmeasured, and often customer-hostile.

**Who experiences it.** Indian subscription & D2C merchants on Razorpay; their ops/finance staff; and their customers (who get either silence or spam).

**Why existing workflows are insufficient.**
- Gateway-level retries re-attempt the same rail; they don't change channel, timing, or message, and don't reason about root cause.
- Bulk SMS/email blasts ignore root cause, annoy customers (complaints, opt-outs, DND violations), and are never measured against a baseline.
- Manual ops (export CSV → agency → pray) doesn't scale, isn't auditable, and can't optimize cost-per-recovery.
- Global dunning tools (Stripe Smart Retries, ChurnBuster, Recurly, Baremetrics Recover) are card-rail + English-first; they don't model UPI failure modes, mandate lifecycles, salary-cycle timing, or vernacular outreach. *(Vendor landscape: [VERIFIED] these products exist; India-rail gap: [ASSUMPTION] based on public product pages — re-verify before citing in pitch.)*

**Why it matters financially.**
- Involuntary churn from failed payments is a documented, large loss category; industry benchmarks place full retry+dunning recovery around 50–70% of failed value, leaving large amounts unrecovered by naive approaches. *(Literature range ~20–48% of churn caused by failed payments, retry-only recovery ~12–18%: [ASSUMPTION] — cite Recurly/Zuora public benchmarks in `docs/` before pitch.)*
- Recovered revenue is ~100% margin — it's the cheapest revenue a merchant can acquire.
- Spam cost is real: complaints, DND/compliance exposure, churn of annoyed customers.

## 4. Why Now

1. **LLM reasoning is now cheap** enough to diagnose messy Indian bank decline strings and write genuinely vernacular (Hinglish) messages at per-message costs of paise.
2. **India rails make it acute:** UPI AutoPay and e-mandate recurring debits are mainstream; their failure modes (de-authorization, per-mandate caps, insufficient funds at debit time) are recovery problems at national scale. `[VERIFIED]` UPI AutoPay/eNACH are real NPCI products.
3. **Agentic infrastructure matured:** tool-calling, structured outputs, and policy evaluation are standard practice.
4. **The buildathon's Track 03 bar** (measured ₹ recovered, compliant escalation, stopping rules, audit trail) is precisely the rigor gap between shipped one-liners and a real product.
5. `[ASSUMPTION — verify before citing]` Razorpay has publicly announced agentic recovery tooling (Subscription Recovery Agent, Intelligent Retry Engine). If verified: Reflex is the **measured, merchant-side, root-cause-specific layer above it** — complementary, not competitive.

## 5. Why Razorpay

Specific, non-generic reasons:

1. **Effective payment success rate is the metric Indian gateways compete on.** Reflex raises merchants' *effective* success rate entirely merchant-side, on top of standard public Razorpay APIs (`[VERIFIED]` Payment Links, Orders, Subscriptions, webhooks exist in test mode). No internal access required.
2. **It extends real Razorpay products:** Subscriptions (UPI AutoPay/e-mandate/card mandates `[VERIFIED]`), Payment Links, webhook events (`payment.failed`, `subscription.charged`, `subscription.halted` `[VERIFIED — verify exact event list against current docs during build]`).
3. **Merchant stickiness:** recovered revenue = pure margin = retention for Razorpay.
4. **The intelligence layer it generates** (root-cause taxonomy + outcome data) is exactly what a Razorpay product team would want next.
5. **Pilot-ready:** works with any merchant already on Razorpay subscriptions — a 10-merchant pilot needs zero new infrastructure.

**Positioning sentence for the pitch:** *"Reflex is the measured, audited merchant-side layer that a one-line product blurb doesn't have to be."*

## 6. Target Users

**Primary persona — Ananya Mehta, 33, Founder/Ops head, "SipDaily"** (demo merchant: chai subscription, ~40k customers, UPI AutoPay + cards + COD).
- **Goals:** cut involuntary churn; don't anger customers; know the ROI of every recovery action.
- **Pain:** ~8–11% of monthly debits fail; current workflow = export failures from Razorpay dashboard → bulk SMS via agency → no idea what worked.
- **Current workflow:** manual, weekly (too late), unmeasured, tone-deaf.
- **How Reflex helps:** autonomous bounded recovery within hours of failure, root-cause-targeted, Hinglish-first, every rupee attributed, one kill switch.

**Secondary persona — Rohit Kulkarni, 41, Finance/Ops manager.**
- **Goals:** audit trail, approvals on anything unusual, reconciliation of recovery costs, compliance safety (DND, quiet hours).
- **Pain:** no visibility into what an automated system did and why.
- **How Reflex helps:** approval queue, hash-chained ledger, cost ledger, suppression list, exported evidence.

**Tertiary — Razorpay solutions/partner engineer (judge/pilot evaluator).**
- **Goals:** evaluate whether this could pilot with real merchants; assess engineering & AI judgment.
- **How Reflex helps:** reproducible eval, clean boundaries, honest failure handling, labeled simulations.

## 7. Core User Jobs

1. *"Recover my failed payments without me doing anything — but never do anything I'd forbid."*
2. *"Tell me why each payment failed and what you did about it."*
3. *"Prove the money you recovered versus what I'd have gotten anyway."*
4. *"Stop the moment a customer is annoyed, and never contact someone who asked us not to."*
5. *"Let me approve/decline unusual or high-value actions myself."*
6. *"Show me an audit trail a bank auditor would accept."*

## 8. Value Proposition

> **Recover more, annoy less, prove everything.** Root-cause-aware, EV-optimized, Hinglish-first recovery orchestration with hard guardrails — measured against a naive baseline on the same batch, every decision logged, every action bounded by non-AI-overridable limits.

## 9. Core User Journey (Ananya, day one)

1. **Connect (5 min):** sign in → point Reflex at her Razorpay **test-mode** credentials → webhook URL registered → import customer context (simulated for demo) → set guardrail defaults (caps, quiet hours, daily budget ₹5,000) → choose **Advisory mode** first.
2. **Observe (day 1):** failures stream in → diagnosis chips appear → actions proposed with EV rationale → she approves a few, watches others auto-run under thresholds → ledger fills.
3. **Trust (day 2):** switches to **Autonomous mode** (still bounded) → watches recovered-₹ counter vs. naive-baseline counter diverge.
4. **Verify (day 3):** opens the Results tab → three-arm comparison, ablations, cost-per-₹100, complaint rate, one-command reproduction.
5. **Intervene as needed:** one complaint arrives mid-episode → Reflex auto-suppresses the customer and files a human-handoff → she resolves it from the approval queue.

## 10. Product Goals

| # | Goal | Measure |
|---|---|---|
| G1 | Recover materially more than naive retry | Incremental value-weighted recovery rate vs B1 (target ≥ +15 pts on replay) |
| G2 | Recover cheaper | Cost per ₹100 recovered (target ≤ ₹3.5 vs B1 ~₹7) |
| G3 | Recover without annoying | Complaint/opt-out rate (target < 0.5% of episodes vs B1 ~2%) |
| G4 | Be safe by construction | 0 guardrail violations in eval; 100% actions ledgered |
| G5 | Be provable | Pre-registered eval reproduced by one command |
| G6 | Survive failures | Degraded-mode recovery ≥ 80% of full-mode recovery |

## 11. Non-Goals

- ❌ Real customer contact (WhatsApp/SMS/telephony providers are **simulated**, labeled `[SIMULATED]`; real integrations are `[PLANNED]` post-MVP).
- ❌ Autonomous refunds, cancellations, or any irreversible money movement.
- ❌ Replacing Razorpay's gateway retries — Reflex orchestrates **above** them.
- ❌ Multi-tenant marketplace, billing, or self-serve onboarding flow for arbitrary merchants.
- ❌ Voice conversation quality beyond one scripted Hinglish recovery call (voice is a demo channel, not the MVP).
- ❌ Fraud detection, chargebacks, reconciliation (other tracks).
- ❌ Working on production/live Razorpay keys.

## 12. MVP Scope

**Must Have:** FR-001–FR-015, FR-017, FR-019, FR-020 (see §13).
**Should Have:** FR-016 full ablation suite (at minimum: EV-off ablation), Hinglish voice call sim, online policy update v2, learning-curve chart.
**Nice to Have:** mandate re-registration journey sim, B2B invoice flavor (PromiseKeeper mini-module), multi-policy profiles.
**Out of Scope:** everything in §11.

## 13. Feature Requirements

| ID | Feature | Description | User | Trigger | Inputs | Processing | Output | Priority | Deps | Acceptance criteria |
|---|---|---|---|---|---|---|---|---|---|---|
| FR-001 | Webhook ingestion | Verified ingestion of Razorpay **test-mode** webhooks | System | Razorpay webhook | Payload + `X-Razorpay-Signature` | HMAC verify → dedup (unique provider event id) → normalize → episode fan-out | Normalized `payment_events` rows | P0 | — | Invalid signature → 401 + security log; duplicate → 200 + dedup counter; no PII in logs |
| FR-002 | Replay engine | Synthetic batch generator + outcome simulator (hidden params), fixed seeds, time acceleration up to 100× | System/Operator | Operator starts replay | n episodes, seed, arm, speed | Generate customers, failure events; simulate customer responses hidden from agent | `replay_batches` + event stream | P0 | Schema | Same seed ⇒ byte-identical batch; simulator params stored but never exposed to agent services |
| FR-003 | Episode lifecycle | Stateful 72h recovery episode per failure | System | First failure event for payment | payment_event | Create episode, attach customer, run state machine (AppFlow §14) | Episode transitions ledgered | P0 | FR-001 | No orphan episodes; every transition written to ledger |
| FR-004 | Rules-based diagnosis | Deterministic canonical root-cause mapping for structured codes | System | Episode created | code_raw, rail, issuer | Regex/lookup → canonical code, confidence=1.0 | `diagnoses` row | P0 | FR-003 | ≥ 70% of synthetic events classified by rules alone; p95 < 100 ms |
| FR-005 | LLM diagnosis (ambiguous tail) | LLM classifies messy decline strings rules can't | System | Rules return UNCATEGORIZED | Redacted context (no PII) | Structured JSON output (canonical code, confidence, rationale) + schema validation | Diagnosis + `llm_calls` row | P0 | FR-004 | 100% schema-valid outputs (retry once, then UNKNOWN_AMBIGUOUS); accuracy ≥ 85% on held-out labeled set |
| FR-006 | EV policy & ranking | Score all candidate interventions: propensity × amount − cost − annoyance penalty | System | Diagnosis complete | Episode context, history, policy version | Compute EV per intervention; rank; select top within guardrails | `candidate_interventions` + chosen action | P0 | FR-005 | EV breakdown (all four terms) stored per candidate; negative-EV ⇒ STOP logged with math |
| FR-007 | Shield guardrails | Deterministic, non-overridable checks: caps (4/episode), contacts (2/customer/day), quiet hours 21:00–09:00 IST, budget ₹5,000/day, suppression list, value>₹50k ⇒ approval | System | Before any action | Action + state | Hard checks in fixed order; fail-closed | PASS / BLOCKED(reason) | P0 | FR-006 | 0 guardrail violations under adversarial test suite; Shield cannot be bypassed by policy module (separate module, tested) |
| FR-008 | Executors & channel sims | Razorpay **test-mode** retry (new order) + Payment Link issuance; simulated WhatsApp/SMS/email/voice delivery + stochastic customer responses | System | Action scheduled | Action | Adapter call; idempotency key; simulated delivery latency & response | Dispatched action + outcome candidate | P0 | FR-007 | Idempotency: replayed dispatch creates zero duplicates; all channels labeled `[SIMULATED]` in UI & ledger |
| FR-009 | Message generation + validator | Hinglish/English tone-band message drafting; **amounts/links/deadlines injected from DB after generation**; validator rejects any digit-bearing LLM span | System | Channel message needed | Episode fields, tone band, lang pref | Template slot skeleton → LLM phrasing → post-injection → validator | Final message (logged pre/post) | P0 | FR-008 | Validator rejects 100% of digit-bearing generated text in adversarial corpus; messages rated (sample) for tone by 3 humans |
| FR-010 | Action ledger & audit drawer | Append-only, hash-chained log of every decision/action/outcome | All | Any state change | Event | seq++, prev_hash → hash; UI timeline | Immutable trail | P0 | Schema | Chain-verification endpoint detects any tampering; drawer shows full episode timeline incl. blocked actions |
| FR-011 | Command-center dashboard | Live failure stream, diagnosis chips, action drawers w/ EV + guardrail state, counters (failed/recovered, Reflex vs naive), charts | Operator | Any | SSE/poll | Live aggregation | Real-time ops view | P0 | FR-003 | Counter delta latency < 2 s at 100× replay; every action's rationale visible in ≤ 2 clicks |
| FR-012 | Human approval queue | Queue + approve/decline with reason; triggers: value > ₹50,000, pause/cancel-class actions, complaint-flagged episodes | Approver | Trigger hit | Action + context | Create approval; hold episode until decision | Decision ledgered | P0 | FR-007 | No action executes while approval pending; timeout ⇒ auto-decline (fail-closed) |
| FR-013 | Kill switch & modes | Halt all actions instantly; modes: Advisory / Autonomous / Degraded / Halted | Operator | Button/API | — | Global flag checked by Shield | Mode banner + ledgered mode change | P0 | FR-007 | Kill switch stops dispatch within 1 s incl. in-flight queue drain; mode changes audited |
| FR-014 | Degraded mode | LLM-outage survival: deterministic fallback diagnosis (rules + UNKNOWN default) + frozen fallback policy; banner + `mode=DEGRADED` on actions | System | LLM health-check fails ×2 | — | Switch paths; queue ambiguous for later | Continued bounded recovery | P0 | FR-005 | Demo: outage switch → zero dropped episodes; degraded actions stamped |
| FR-015 | Outcome observation & attribution | Watch windows per action (default 6h sim); attribute recovery to last action; credit assignment to policy | System | Timer/dispatch | Events | Match success event to episode+action | `outcomes` rows | P0 | FR-008 | 100% episodes reach terminal state within 72h sim; attribution unambiguous (one open action at a time) |
| FR-016 | Baselines B0/B1 | B0 = do nothing (organic recovery only); B1 = **tuned naive** (immediate same-rail retry ×3 + generic SMS blast ×2) run on identical batch | Eval | Eval run | Batch | Same simulator, different policy | Arm results | P0 | FR-002 | B1 honestly tuned (documented tuning); identical seeds/batch across arms |
| FR-017 | Eval harness & ablations | Pre-registered protocol (git tag `eval-preregistered-v1`), N=3,000, seeds {42,1337,2025}, bootstrap 95% CI (1,000 resamples), ablations A1–A4, `./reproduce.sh` | Eval | Command | Config | Run arms + ablations | Metrics tables + JSON | P0 | FR-016 | Fresh clone → one command → results within tolerance; protocol file committed **before** first results run |
| FR-018 | Suppression & complaint handling | Reply classifier (COMPLAINT ⇒ instant global suppression + human handoff; OPTOUT ⇒ permanent) | System | Simulated reply | Message | Rules+LLM classify | Suppression row + episode STOPPED_CUSTOMER | P1 | FR-009 | Zero post-complaint contacts in eval; classifier ≥ 95% precision on COMPLAIN class |
| FR-019 | Metrics views | Live + eval tabs: recovery rate, incremental ₹, cost/₹100, complaint rate, time-to-recover, contacts/recovery, latency, degraded-mode delta, learning curve | All | Tab | Aggregations | Compute + chart | Tables/charts | P0 | FR-015 | Every eval number shows CI + arm + seed; all sim results labeled `[SIMULATED]` |
| FR-020 | Demo & failure-injection controls | One-click: start demo slice (214 episodes, ₹2,41,000), arm toggles, 100× speed, inject LLM outage, inject webhook storm, inject complaint | Operator | Demo mode | — | Scenario scripts | Deterministic demo | P0 | FR-014 | Demo runs end-to-end in ≤ 10 min real time; every injection recovers visibly |

## 14. AI Requirements

| # | Component | Why AI needed | What it does | Input | Output | Model/system | Evaluation | Failure behavior | Human oversight |
|---|---|---|---|---|---|---|---|---|---|
| AI-1 | **Diagnosis NLU** | Same cause surfaces as different bank strings across issuers; a lookup table can't cover the tail | Canonical root-cause classification of messy decline strings | Redacted context (raw string, rail, masked issuer, history) — no PII | `{canonical_code, confidence∈[0,1], rationale}` strict JSON | Hosted LLM (OpenAI-compatible; model configurable; MVP tested with a GPT-4o-mini-class model) | Held-out labeled set of 500 ambiguous cases; target accuracy ≥ 85%; confusion matrix published | confidence < 0.6 ⇒ `UNKNOWN_AMBIGUOUS` + conservative default action; invalid JSON ⇒ 1 retry ⇒ fallback | None required; all calls logged |
| AI-2 | **EV policy** | Choosing *whether/when/how* to intervene is prediction + optimization under uncertainty; fixed rules are the baseline we must beat | Propensity-to-recover scoring (logistic regression, batch-retrained; v1 = literature-calibrated priors) + EV arithmetic per candidate intervention | Episode features (root cause, amount band, rail, history, hour, day-of-month, prior contacts) | Ranked interventions w/ EV breakdown | Logistic regression (v1 prior-frozen; v2 learned from replay outcomes) | Policy regret vs. simulator oracle; arm comparison; ablation A2 (fixed-priority) | Model unavailable ⇒ frozen v1 policy; EV < 0 ⇒ STOP (by design, not failure) | Thresholds/caps set by operator; Shield overrides |
| AI-3 | **Message generation** | Empathy + tone + genuine Hinglish phrasing at scale; templates are the baseline | Phrasing variant generation within tone bands (gentle/firm/urgent) for slot-skeleton messages | Slots (amount, due date, link placeholder), lang pref, tone band, contact count | Message text **without any digits/links/amounts** (injected after) | Hosted LLM | Validator pass rate (100% digit-free required); A/B in replay (personalized vs static = ablation A3); 3-human tone rating sample | Any violation ⇒ template fallback; validator rejection logged with diff | None; all drafts logged pre/post injection |
| AI-4 | **Reply/complaint classifier** | Free-text/voice-transcript replies need intent understanding; missing a complaint is a trust-killer | Classify simulated replies: PROMISE / REFUSE / COMPLAINT / OPTOUT / PAYING / AMBIGUOUS | Reply text (simulated), episode context | Intent + extracted promise date if any | Hosted LLM (or rules+LLM hybrid) | Labeled set of 300 replies; COMPLAIN precision ≥ 95% | AMBIGUOUS ⇒ treat as non-response (safe default); COMPLAINT path is rule-gated too | Complaints always create human-handoff |

**Where AI is deliberately NOT used (product-level rule):** EV arithmetic, caps/budgets/quiet hours, scheduling math, retry/link execution, ledger writes, compliance filtering, idempotency — all deterministic. See `8. Rules.md` §2.

## 15. Agentic Requirements

**Claim:** the Episode Agent is a genuine bounded agent — not a workflow labeled "agent." Justification against criteria:

| Criterion | How Reflex meets it |
|---|---|
| Planning | Bounded horizon-1: enumerate candidate interventions, rank by EV, schedule time-shifted top action. **Deliberate design (ADR-002, TechSpec §20):** open-ended multi-step planning is inappropriate for money actions; horizon-1 + observation loop achieves the same adaptivity with auditable decisions |
| Tool use | Allowlisted tools only: RP-TM create order, RP-TM payment link, channel sims, ledger write, policy read, suppression write |
| State | `episodes` + `actions` (AppFlow §14 state machines) |
| Memory | Episode-scoped (contact history, prior outcomes); customer-scoped (propensity features, suppression); policy-level (learned params) |
| Decision-making | EV policy (AI-2) within Shield constraints |
| Verification | Shield (pre-dispatch), message validator (AI-3), outcome attribution (FR-015) |
| Recovery | LLM outage ⇒ degraded mode; executor timeout ⇒ backoff ×3 ⇒ park + alert |
| Escalation | Human approval queue (value/pause/complaint triggers); ALWAYS for pause/cancel-class |
| Human approval | FR-012 gates; timeout auto-declines (fail-closed) |
| Action boundaries | Caps: 4 actions/episode, 2 contacts/customer/day, ₹5,000/day budget, quiet hours 21:00–09:00 IST, suppression list, kill switch |
| Stop conditions | RECOVERED · EXPIRED (72h) · cap reached · EV < 0 (STOPPED_LOW_EV) · complaint/opt-out (STOPPED_CUSTOMER) · kill switch (HALTED) · approval declined |
| Audit trail | Hash-chained action ledger, every state change |

**Deterministic components explicitly NOT called agents:** Shield, executors, ledger, replay engine.

## 16. Business Metrics

> **STATUS (v1.1):** The table below contains **design targets only**. As of this sync **no
> official evaluation has been executed**; `eval/results/` holds only the committed B1 tuning
> search (`b1_tuning.json`). The largest run to date is a **non-official smoke** (seed 42,
> n=60, single seed — NOT citable as results): b0 ₹1,102 / b1 ₹2,215 / reflex ₹5,103 recovered
> of ₹22,938 failed value ⇒ 4.8% / 9.7% / 22.2%, with ordering b0 < b1 < reflex and ablations
> A1≡reflex offline (no LLM configured). These smoke numbers must never be presented as results.

**Primary KPI:** **Incremental value-weighted recovery rate & incremental ₹ recovered vs. tuned naive baseline (B1)** on pre-registered replay. Definition: `recovered_value(arm) / failed_value(batch)` within 72h sim window; incremental = Reflex − B1, with bootstrap 95% CI.

**Simulation design targets (actuals produced by eval; always labeled `[SIMULATED]`):**

| Arm | Recovery rate | Cost / ₹100 recovered | Complaint rate | Median time-to-recover |
|---|---|---|---|---|
| B0 do-nothing | ~7% | ₹0 | ~0% | — (organic) |
| B1 tuned naive | ~24% | ~₹6.9 | ~1.9% | ~26h |
| **Reflex** | **~42%** | **~₹3.0** | **<0.5%** | ~9h |

**Secondary KPIs:** contacts per recovery · escalation precision · p95 decision latency (rules < 1.5 s / LLM < 6 s) · degraded-mode recovery delta · learning-curve slope (v1→v2 policy) · ablation deltas (A1 rules-only dx; A2 fixed-priority policy; A3 static templates; A4 no timing optimization).

**Baseline:** B0 and B1 as above; B1 is **tuned** (documented parameter search) — never a strawman.

**Measurement methodology:** pre-registered protocol (`eval/PROTOCOL.md`, git-tagged before first run); N=3,000 episodes; seeds {42, 1337, 2025}; identical batch across arms; bootstrap 1,000 resamples; `./reproduce.sh` from clean clone; one honestly-reported cohort where Reflex **correctly declines to act** (low-value ephemeral failures where contact cost > EV).

## 17. Security & Trust Requirements

1. Webhook HMAC verification (`X-Razorpay-Signature` `[VERIFIED header]`); invalid ⇒ 401 + security event.
2. RBAC: `viewer` (read-only) / `operator` (controls) / `approver` (queue) / `admin`. Server-side enforcement only.
3. Secrets via environment, never in repo; test-mode keys only.
4. PII minimization: pseudonymous customer IDs, masked VPAs/cards; **no PII in LLM prompts or logs**.
5. Rate limiting on all API routes; idempotency keys on every mutating endpoint.
6. Hash-chained ledger + `/api/ledger/verify` endpoint.
7. Prompt-injection defense: untrusted fields (bank strings, replies) wrapped in delimiters + "data, not instructions" contract; schema-validated outputs; tool allowlist.
8. Kill switch reachable in ≤ 1 click from every screen.
9. Fail-closed semantics everywhere uncertain.

## 18. Failure Handling (product-level; details in AppFlow §8)

| Failure | Product behavior |
|---|---|
| LLM outage | Degraded mode banner; deterministic fallback; recovery continues ~9 pts lower; all actions stamped `DEGRADED` |
| Webhook storm/duplicates | Idempotency collapse ("1,000 events → 214 episodes" shown live) |
| Complaint mid-episode | Instant suppression + human handoff; zero further contact |
| Razorpay test API timeout | Backoff ×3 → park `action_pending` + alert; no lost actions |
| LLM hallucinates an amount | Validator strips/rejects; template fallback; rejection logged with diff |
| Budget exhausted | Paid actions stop; free channels only; ledgered budget event |

## 19. User Stories

1. As **Ananya**, I want failures diagnosed automatically so I stop guessing why debits fail.
2. As **Ananya**, I want one screen showing recovered ₹ vs. what naive retry would have gotten.
3. As **Ananya**, I want customers messaged in Hinglish at sensible hours so recovery doesn't cost goodwill.
4. As **Ananya**, I want a kill switch that stops everything instantly.
5. As **Rohit**, I want to approve anything above ₹50,000 or unusual before it's sent.
6. As **Rohit**, I want an immutable audit trail of every action with its justification.
7. As **Rohit**, I want complaints to auto-stop all contact and land in a human queue.
8. As **Ananya**, I want the system to know when *not* to act (low value, high annoyance cost).
9. As a **judge/engineer**, I want to reproduce every metric with one command.
10. As **Ananya**, I want the system to keep working (safely) when the LLM provider is down.

## 20. Acceptance Criteria (release gate)

> **v1.1 STATUS ANNOTATIONS** — current verified state per criterion:

1. ⏳ End-to-end golden path: webhook → diagnosis → EV action → Shield pass → simulated send → outcome → ledger — under 5 s sim-time. *(VERIFIED in-process via shared pipeline code + integration test incl. ledger/attribution; live HTTP+workers pass NOT yet executed.)*
2. ❌ Three-arm eval executed + results with CIs committed; protocol tag predates results. *(Tag predates ✓; official run BLOCKED — harness proven at smoke scale only.)*
3. ✅ Zero Shield violations under adversarial test suite (attempted bypass: over-cap, quiet-hours, suppressed customer, post-kill-switch dispatch). *(Unit suite green incl. at-limit boundaries; eval runs recorded 0 violations with blocks/approvals observed.)*
4. 🟡 Degraded-mode demo runs with zero dropped episodes. *(DEGRADED arm completes in-process with stamps; live outage-injection drill pending.)*
5. ✅ Validator rejects 100% of digit-bearing LLM output corpus. *(14-span adversarial corpus, CI-level unit test green.)*
6. ✅ Ledger chain verification passes on all runs; deliberate tamper test detected. *(Unit + integration verify_db green across golden-path run.)*
7. ✅ All simulated data labeled `[SIMULATED]` in UI, ledger, exports, README. *(UI/ledger/eval artifacts labeled; exports themselves not yet built; README absent.)*
8. ❌ 60-second wow — requires live stack demo (not yet performed).
9. 🟡 `./reproduce.sh` works from clean clone in < 15 min. *(Script written; never executed end-to-end.)*
10. 🟡 No secrets in repo (only `.env.example`; gitleaks configured but scan not yet executed); RBAC enforced server-side (verified by tests ✅).

## 21. Future Expansion

- Real channels: WhatsApp Business API, SMS/telephony partners `[PLANNED]` — behind the same Shield & message validator.
- Mandate re-registration journeys (deep links + assisted flows) for MANDATE_REVOKED.
- Uplift-modeled discounting (only discount would-be-lost recoveries).
- Cold-start priors from aggregate (anonymized) failure/outcome stats — a natural Razorpay-side asset.
- Multi-merchant policy profiles & A/B testing harness for live pilots.
- B2B receivables flavor (promise-to-pay state machine) — architecture already supports episodes on invoices.
---

## 22. Implementation Status Snapshot (v1.1 — synced to codebase)

**Counts:** 26 tasks ✅ DONE · 20 🟡 PARTIAL · 6 ❌ NOT STARTED (of 52). Effective ≈ 69%.

### FR status map
| FR | Status | Note |
|---|---|---|
| FR-001 webhook ingestion | IMPLEMENTED | HMAC 401 path, dup→200, zero-amount reject — all tested |
| FR-002 replay engine | IMPLEMENTED | byte-identical seeds tested; hidden params role-gated |
| FR-003 episode lifecycle | IMPLEMENTED | terminals enforced by code + DB triggers |
| FR-004 rules diagnosis | IMPLEMENTED | 88% corpus coverage (>70% gate) |
| FR-005 LLM tail | PARTIAL | full path works LLM-absent; ≥85% holdout gate requires LLM key (skipped honestly) |
| FR-006 EV policy | IMPLEMENTED | 4-term breakdown persisted; negative-EV STOP logged with math |
| FR-007 Shield | IMPLEMENTED | adversarial suite green incl. at-limit edges; isolation lint green |
| FR-008 executors+sims | PARTIAL | sims deterministic+tested; RP-TM client coded but never called live (no keys) |
| FR-009 messages+validator | IMPLEMENTED | validator rejects 100% digit corpus; template fallback exercised every offline run |
| FR-010 ledger+drawer | IMPLEMENTED | tamper detection tested; UI drawer built (live click-through pending) |
| FR-011 dashboard | PARTIAL | built+compiles; SSE live round-trip not yet demonstrated |
| FR-012 approvals | IMPLEMENTED | triggers, timeout auto-decline observed in runs, UI page |
| FR-013 kill switch/modes | PARTIAL | backend+UI done; ≤1s drain not yet measured live |
| FR-014 degraded mode | PARTIAL | DEGRADED arm proven in-process (stamps, zero drops); live drill pending |
| FR-015 outcomes/attribution | IMPLEMENTED | golden-path integration: 100% terminals, single recovery, organic vs action split |
| FR-016 B0/B1 | IMPLEMENTED | same-batch arms; B1 tuning search committed (dev seeds) |
| FR-017 eval harness+ablations | PARTIAL | runner+CIs proven ≤ n=120 smoke incl. A1–A4+DEGRADED; official N=3000×3 BLOCKED on host Docker instability (code-side causes fixed) |
| FR-018 suppression/complaints | IMPLEMENTED | keyword gate first; COMPLAIN precision ≥95% offline gate green |
| FR-019 metrics views | PARTIAL | /metrics/live·eval implemented; Results page built; not viewed together with data |
| FR-020 demo controls | PARTIAL | inject endpoints real-path implemented; none exercised live |

### AI components
AI-1 🟡 (offline-safe paths verified; accuracy gate needs key) · AI-2 ✅ v1 priors (v2 trainer written, never run) · AI-3 ✅ (validator gate) · AI-4 ✅ offline precision gate.

### Known implementation limitations (honesty record)
1. Official evaluation results do not exist yet — nothing may be quoted as a result.
2. Live HTTP+workers demo loop unverified after worker bugfix (_mode NameError fixed post-smoke).
3. Razorpay test-mode APIs never actually called (no keys); connector is fail-closed without keys.
4. Exports (CSV/JSON labeled) not built; README/LICENSE/docs/ absent.
5. Backend suite green (119/120) at commit 115a5c0 is STALE vs HEAD — re-run required.
6. A1 ≡ Reflex offline (no LLM configured): expected and must be disclosed in results.
